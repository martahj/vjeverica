import React from 'react';
import {mount, shallow, render} from 'enzyme';
import {expect} from 'chai';

// const browserHistory = Router.browserHistory;

import ClientRoutes from '../../routing/clientRoutes.jsx';

describe('<ClientRoutes />', function() {

	it('should return true', function() {

		// const wrapper = shallow(<ClientRoutes />);
		// console.log('wrapper in shallow app', wrapper);
		// console.log('wrapper props', wrapper.props);

		// const rendered = mount(<ClientRoutes />, {attachTo: document.getElementById('root')} );
		// console.log('rendered app', rendered);
		// console.log('rendered props', rendered.props);

		// const mounted = mount(<ClientRoutes />);
		// console.log('mounted app', mounted);
		// console.log('mounted app props', mounted.props);

		expect(true).to.be.true;
	})
})